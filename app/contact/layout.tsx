import { LayoutProps } from "@types";
import React from "react";

export const metadata = {
  title: "Contact - Custom Printing",
  description: "Reach out to us",
};

const ContactLayout = ({ children }: LayoutProps) => {
  return <>{children}</>;
};

export default ContactLayout;
